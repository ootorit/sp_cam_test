# CLAUDE.md

This file provides guidance for AI assistants working with the sp_cam_test repository.

## Project Overview

**sp_cam_test** is a newly initialized repository. No framework, language, or build system has been selected yet. The project structure is minimal.

## Repository Structure

```
sp_cam_test/
├── README.md        # Project readme (placeholder)
└── CLAUDE.md        # This file - AI assistant guidance
```

## Git Workflow

- **Primary branch**: `main` (remote) / `master` (local)
- **Feature branches**: Use the pattern `claude/<description>-<id>` for AI-assisted work
- **Commits**: Signed with SSH keys; write clear, descriptive commit messages
- **Push**: Always use `git push -u origin <branch-name>`

## Development Guidelines

- Keep the codebase simple; avoid over-engineering
- Do not add files, dependencies, or abstractions beyond what is needed
- Update this CLAUDE.md as the project evolves with new tooling, conventions, or structure
